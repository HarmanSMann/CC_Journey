#include <stdio.h>

float sum(float, float); // declare a function before hand

int main()
{
  float x, y;
  x = 10.0;
  y = 11.1;
  
  result = sum(x, y);
  printf("%f", result);
}

float sum(float i, float j) // write it out after main
{
 return i + j; 
}
